# dropper-app — イベントドロッパー（ツール本体）

GitHub Pages で公開している静的サイト。サーバーは無い。**リポジトリは public**（内部メモ・秘密情報をコミットしないこと）。
公開URL: https://app.dropper-tools.com/calendar/ （en: /calendar-en/ 、Hinglish: /calendar-in/ ）

## 名称について

- ツールの表示名は **「イベントドロッパー」**（en/in: Event Dropper）。
  2026-07-26 に旧称「カレンダードロッパー」から変更（周知サイト側は先行変更済み）。
- Google OAuth 登録のアプリ名 **「ドロッパー」は別物**。こちらを変えると再審査になるため触らない。
- 案内文のクレジット行（`i18n.js` の `annCredit`）の「ドロッパー」は**シリーズ名として意図的**。
  表示名と揃えなくてよい。ドロッパーに着地すれば**予定表ドロッパーへも誘導できる**ため。
  「表示名は イベントドロッパー なのに」と直さないこと。

## 構成

- `calendar/` `calendar-en/` `calendar-in/` — 3言語版のツール
- `add/index.html` — 案内文リンク用の中継ページ（言語共通）
- `attend/` — 出欠ドロッパー（回答・集計ページとセットアップウィザード。言語共通・日本語のみ）

このリポジトリにはドロッパーが**3本**入っている。用途が重ならないよう分けてある。

| 本体 | フォルダ | 1枚から出てくるもの |
|---|---|---|
| イベントドロッパー | `calendar*` | 予定**1件** ＋ 案内文 |
| 予定表ドロッパー | `schedule*` | 予定を**何件も** |
| 決めごとドロッパー | `decide*` | 決まったこと／未決／やること／記録に**仕分け** |

## イベント種類（DROPPER_TYPES）

`app.js` の `DROPPER_TYPES` に6種類を定義（sports / music / exhibition / lecture / festival / general）。
**種類を増やすときは、この配列に1件足し、i18n に ja/en/in 3言語のキーを足すだけ**でよい。

- `labels` … カード欄ラベルの上書き（slot: name / format / opening / deadline）
- `aiHint` … AI抽出プロンプトへ差し込む種類別の補足（`{ja, en}`）。
  **プロンプト全文は複製しない**。共通の汎用プロンプトにこの一文だけを足す設計。
- `dateRange` … 会期（初日〜最終日）で扱う種類。日付行を「初日/最終日」にし、案内文も期間1行にする。
- `longRun` … 会期が長いのが普通の種類。「日付が離れすぎ」警告を出さない（展示会用）。
- `annEmoji` … 案内文の先頭絵文字。

スポーツのみ `useSportSelector: true`（競技セレクタ表示＋競技用プロンプト）。
それ以外は `parser.js` の**汎用イベントモード**（`parse(text, sport, eventMode, opts)` の第3引数）で解析する。

## 抽出でハマった実例（parser.js）

実チラシで判明した対策。**消すと再発する**:

- **NFKC正規化** — Word製PDFは「康熙部首」（⽇ は 日 と別文字）や縦書き用記号（︓）が混ざり、
  そのままでは日付・時刻が一切拾えない。
- **漢数字** — 神社仏閣・式典の案内は「令和五年十一月三日」「午後一時」と書かれる。
  年月日・時分の直前に限って変換する（「九州」「三菱」を壊さないため）。
- **西暦・和暦の併記** — 「2026（令和8）年11月15日」は括弧側を落としてから解析する。
- **文章の行を除外** — あいさつ文に施設名や催し名が出るため、会場・名称の候補から
  「句点を含む」「敬体の語尾（〜ます等）」「極端に長い」行を外す。
  格式ある案内状は句点を打たないので、語尾と長さの判定が要る。
- **発売日 ≠ 締切** — チケット発売日を開催日・申込締切と誤認しない。
- **閉じ括弧** — 対応が取れている括弧は残す（個展「あること」が壊れる）。

## 3言語同期の鉄則

**3本のドロッパー（`calendar*` `schedule*` `decide*`）すべてに効く。**

- `app.js` `parser.js` `i18n.js` は **3フォルダで完全同一**（バイト単位で同じ）。片方だけ直さない。
  予定表側も同じで、`schedule-i18n.js` `schedule-parser.js` `schedule-ai.js` `schedule-app.js` の4本。
  決めごと側は `decide-i18n.js` `decide-ai.js` `decide-app.js` の3本。
- `index.html` は **`window.LANG` の値だけが違う**。丸ごとコピーすると壊れる。追加部分だけ差し込むこと。
- 検証方法: `index.html` の `window.LANG` を置換して diff を取り、一致すればOK。
- en/in を作り直すときは `cp` でバイト複製してから `window.LANG` の行だけ置換する。
  Git Bash の `sed > file` は **CR を落として LF にする**ため、バイト同一でなくなる。
- `i18n.js` に文言を足すときは **ja / en / in すべてに同じキー**を足す。
- **`window.LANG` を読むスクリプトは、必ず `window.LANG` を設定したあとに置く。**
  訪問者カウンター（hits.sh）がヘッダー内にあり、`window.LANG` が未定義のまま走っていたため、
  **en/in の訪問が3言語とも ja の数に混ざっていた**（2026-07-29 に修正）。
  画面上は正常に見え、数字が混ざるだけなので気づきにくい。両ツールとも同じ形にしてある。

## ドロッパー切り替えタブ（3本の行き来）

ヘッダーの h1 の上に `.tool-tabs` を置き、イベント／予定表／決めごとを行き来させる。

- **3言語同時**に触る。文言は各辞書の
  `typePrompt`（上段ラベル）/ `toolEvent` / `toolSchedule` / `toolDecide` / `toolHint`。
  下段の「イベントの種類」は `eventTypePrompt`。
- **リンク先は同じ言語の版へ送ること。** 3本とも `applyDom` が言語に合わせて `href` を張り替える
  （自分以外の2本 = `#tabEvent` / `#tabSchedule` / `#tabDecide`）。HTMLに直書きの `href` は ja 用の既定値。
  en の利用者を ja のページへ飛ばさないための仕組みなので、外さないこと。
- タブの CSS は3本の `index.html` に同じものを複製している。**1本だけ直さない**。
- 種類選択後の表示に「ドロッパー」は付けない（タブ・h1 と合わせて3重になるため）。
  旧 `dropperSuffix` キーは削除済み。

## 「何ができる？」ポップアップ（`#tools-modal`）

2026-08-04 追加。周知サイトには3本それぞれの説明があるのに、ツール本体はタブと1行のヒントだけで
**何ができるのかが見えなかった**。タブ列の右端の `#whatBtn` から開き、3本の入力と出力を図で見せる。

- **タブ自体は今までどおり直行させる。** 「タブを押す → 説明 → それから移動」にすると、
  行き来する人に**毎回1クリック増える**。説明はボタンに分けてある。
- **初回だけ自動で開く**（`localStorage` の `dropper_tools_seen`）。2回目以降はボタンからのみ。
  AI利用ポップアップはログイン後に出るので重ならないが、既に開いていたら譲るようにしてある。
- **図は SVG ではなく HTML＋CSS で組む。** ラベルが普通のテキストなので `data-i18n` がそのまま効き、
  英語で文字幅が伸びても折り返せる。SVG の `<text>` だと英語で溢れる。
- **図は「1枚から何がいくつ出るか」だけを描く。** ここが3本の違いのすべて。
  イベント＝マスが1つだけ光る／予定表＝マスが多数＋間に「一覧で確認」／決めごと＝仕分け箱4つが主役。
- **いま開いているドロッパーはリンクにしない**（`.tm-here`「いま使っています」）。タブの `.on` と同じ考え方。
- **どれが自分かは `#tools-modal` の `data-home` 属性が持つ。** これで開閉・タブ切替のJSが
  3本で同一に保てる（`app.js` / `schedule-app.js` / `decide-app.js` に同じブロックがある）。
  **1本だけ直さない**。CSS・マークアップも9本の `index.html` で同じもの。
- 「使ってみる」の `href` も `applyDom` が言語別に張り替える（`#tmGoEvent` / `#tmGoSchedule` / `#tmGoDecide`）。
  タブと同じ規則なので、**リンク先を変えるときは両方そろえる**。
- 文言キーは `tm*`（共通32＋自分以外の2本へのリンク2）。**3つの辞書それぞれに重複させてある。**
  辞書を跨がせない既存の決まり（下の `schedule-i18n.js` の項）を守るため。

---

# 予定表ドロッパー（`schedule/`）— ドロッパーシリーズ第2弾

2026-07-27 に着手。**1枚の予定表から N件の予定を取り出し、カレンダーへ一括登録する**ツール。
イベントドロッパーが「1枚＝1イベント」なのに対し、こちらは「1枚＝N件」。用途が重ならない。

公開URL: https://app.dropper-tools.com/schedule/ （en: /schedule-en/ 、Hinglish: /schedule-in/ ）

## 構成

`schedule/` `schedule-en/` `schedule-in/` の3フォルダ。**イベントドロッパーと同じ同期の決まりが効く**
（下の「3言語同期の鉄則」参照）。`deploy-check` の対象はこの3フォルダも含む。

- `schedule/index.html` — 画面（一覧表・形の選択・年度入力・APIキー入力）
- `schedule/schedule-i18n.js` — 多言語辞書（`window.I18N`）。**イベント側の `i18n.js` とは別物**。
  あちらは3フォルダでバイト同一を保つ決まりで、予定表のキーを足すと2つのツールが結合するため。
- `schedule/schedule-parser.js` — 通常モードの抽出（`window.SchedParser.parse(text, opts)`）。
  **`opts.lang` を必ず渡すこと**。渡さないと英語の日付・時刻を一切拾わない。
- `schedule/schedule-ai.js` — AIモード（`window.SchedAI.extract(file, opts)`）
- `schedule/schedule-app.js` — 画面制御・OCR・カレンダー一括登録
- `schedule/test/` — **実機のOCRテキストと確認用スクリプト**（後述）

**エラーの判定に文言を使わないこと。** 401の期限切れは合言葉（`throw new Error('expired')`）で判定する。
以前は日本語の文言を `indexOf('期限切れ')` で見ており、多言語化した時点で英語版だけ
セッション切れを検出できなくなる状態だった。
- `schedule/test/` — **実機のOCRテキストと確認用スクリプト**。`schedule-parser.js` を触ったら
  `node schedule/test/run.js` を修正の前後で流し、件数と中身を見比べる（詳細は同フォルダの README）

**OAuthはイベントドロッパーと共有**（同じクライアントID・同じ生成元 `app.dropper-tools.com`）。
スコープも同じ3つだけ。**新しいスコープは足さない**。APIキーの保存先も共通（`dropper_ai_key`）。

## 多言語化の決まり（`schedule-i18n.js`）

- キーは **ja / en / in の3言語すべてに足す**（現在91キー）。イベント側と同じ `data-i18n` 方式。
- **年度欄の意味が言語で変わる。** ja は「年度」（4月始まり）、en/in は「年」（暦年）。
  文言（`fyLabel` / `fyHint`）だけでなく、`schedule-parser.js` の扱いも分かれている。
- 形の説明文の改行は**辞書側の `\n`**で表す（CSS は `white-space:pre-line`）。
  `<br>` は辞書に入らないうえ、言語ごとに折り返し位置が違うため。

## 2つのモード（役割が違う）

- **通常モード** … GoogleドライブのOCR → テキスト → `schedule-parser.js` で抽出。表形式の予定表向け。
- **AIモード** … OCRを経由せず、**PDF・画像そのものを Gemini に渡す**。
  マス目のカレンダー（学校の年間行事予定表など）は、テキスト化した時点で日付と行事名の
  対応が失われるため、これ以外に手が無い。実データで157件・精度100%を確認済み。

## マス目のカレンダーは、聞かずに見分ける（`looksLikeGrid`）

画面はイベントドロッパーと同じ **「通常モード／AIモード」の2択だけ**。予定表の形は聞かない。

一時期「表形式か、マス目か」を図で選ばせていたが、**選び間違えたことに誰も気づけない**のが問題だった。
マス目をOCRで読むと0件にはならず、**それらしい件数のまま日付と行事名の対応だけが狂った結果**が出る。
いまは通常モードで読んだ**あと**に `schedule-parser.js` の `looksLikeGrid(text)` で見分けて警告する。

- **信号は3つ。どれか1つでも立てばマス目とみなす**（`schedule/test/` の実データ8枚で測定）。
  - 月名だけの行が**4つ以上連続**（`4月 5月 6月 …` のマトリクス型の見出し）
  - 「**数字だけの行 → 曜日だけの行**」の組が**20以上**（マス目の中身）
  - 曜日だけの行が**5つ以上連続**、または1行に曜日が5つ以上（`日 月 火 水 木 金 土` の見出し）
- **3つとも残すこと。** 曜日が日付と交互に来るマス目があり、曜日の連続だけでは取りこぼす。
  ローカルのPDF抽出では曜日がまとまって見えたため、実物のOCRを見るまで気づけなかった。
- 判定の余裕: 表形式は全項目で最大1、マス目は最小6。しきい値の両側に幅がある。
- **見分けても勝手にAIへ切り替えない。** 利用者のAPIキーの無料枠を消費するため、
  警告（`#gridWarn`）を出して「AIで読み直す」を押すかどうかは委ねる。
- 通常モードで読んだあとは **「AIで読み直す」**（`#retryAi`）を出す。モードを切り替えずに同じ
  ファイルをAIへ回せる逃げ道。`handleFile(file, forceAi)` の第2引数で、1回だけAIを使う。
- 案内文（`#ocrNote`）はモードごとに `schedule-app.js` の `setMode` が書き換える。

## AI利用の説明ポップアップ（`#ai-modal`）

「AIモード」を押すと出る。イベントドロッパーの同名ポップアップと**同じ内容・同じ流れ**。

- **キーを求める前に必ず説明を見せる。**「キーは端末内にしか保存しない」「無料枠が1日約1,500件」を
  読まないままキーを入力させない。だから**AIモードへの入口はこのポップアップ1つに統一**してある。
- **「APIキーを変更」の専用ボタンは置かない。** キーの入れ直しは「AIモード」をもう一度押す
  （すでにAIモードでも入力欄が出る）。ボタンを足すと説明を飛ばす経路ができるので、足さないこと。
- キー入力をキャンセルしたら**ポップアップは閉じない**（選び直しを促す）。
- 「AIで読み直す」も、キーが未設定ならまずこのポップアップを出す。その分キー未設定の人は
  2回押すことになるが、**押していないのに無料枠を消費しない**ことを優先している。

## 抽出でハマった実例（消すと再発する）

- **GoogleのOCRは表を「1セル＝1行」で返す** — 最重要。日付だけの行が続き、行事名は次の行以降に来る。
  日付行に本文が無いときは、次の日付が現れるまでの行をまとめて行事名にしている。
  ※ローカルのPDFテキスト抽出（pypdf等）とは並びが違うので、**必ず実機のOCR結果で確かめること**
  （採取済みのものが `schedule/test/` にある。片方の予定表だけ見て直し、もう片方の形式を
  見落としたことがある）。
  読み取り0件のとき画面に「読み取れた文字」を出す仕組みを入れてある（`window.lastOcrText`）。
- **年度から西暦を決める** — 4〜12月は年度の年、1〜3月は翌年。全角の「２０２５年度」「令和８年度」は
  NFKC正規化しないと拾えない。年の記載が無い予定表があるので、年度の手入力欄は必須。
- **日付が次の語を食べる** — `25/3/19 ～22 日本選手権` で「日本」の日を消費して「本選手権」になった。
  スラッシュ表記と漢字表記で正規表現を分けて解決。
- **「3時間授業」の「3時」を時刻と誤認** — `時` の後ろが `間` なら時刻としない。
- **場所の書かれ方は2通りある**（`PREF_HEAD_RE` / `PREF_ONLY_RE`）— 分けないと会場名が行事名に混ざり、
  **カレンダーの「場所」が空**になって地図が出ない。都道府県は場所側に含める（同名の体育館が各地にある）。
  - `埼玉県 深谷市他` … 1行に都道府県つき。**その行から後ろが場所**（全国大会予定表）
  - `高知` ＋ `高知県民体育館` … 県名だけの行と会場の行に分かれる（四国大会予定表）
  - **県名だけの表記は、後ろから2行以内に来たときだけ拾うこと。** Tリーグ日程表は
    `静岡` `京都` `岡山` が**チーム名**として1セルに入っており、素朴に拾うと
    `KM東京 - 岡山` の `岡山` を場所の始まりと誤認して行事名が `KM東京 -` だけになる。
  - 先頭の行が場所に見えるときは分けない（行事名が消えてしまうため）。
- **区切りを見落とすと次の行が吸い込まれる** — 日付の無い行は次の日付まで本文として拾うため、
  以下は明示的に区切る。実物で見つけたものを足していくこと。
  - `未定` の行（日付の無い別の予定）／英語は `TBC` `TBA`
  - `◯◯予定表` `◯◯日程表`（次の表の見出し。1つの文書に表が何枚も入っている）
  - 表の見出し語（`HEADER_RE`）。`試合日 対戦` が予定に混ざって見つかった。
  - 月名だけの行（`EN_MONTH_ONLY_RE`）。`JANUARY 2026` の見出しと、日が未定の `January` の両方。
  - 電話番号・メール・URLだけの行（`CONTACT_RE`）。連絡先の列を持つ表がある（ASA）。
    ※これを飛ばすと収集範囲が伸びて**次の行を飲み込む**ようになる。月名の区切りとセットで入れること。
  - 曜日だけ・曜日の範囲だけの行（`EN_WD_LINE_RE`）。`SAT – SUN` `THURS -` と改行で割れることもある。
- **日付より前の文字も行事名に入れる** — 日付が行の途中にある予定表がある
  （IPLは `Kolkata Knight Riders 1st Match Sun Mar 15, 2026 …` とチーム名が先）。
  ここを捨てると**対戦相手の片方が消える**。日付が行頭の予定表には影響しない（実測で確認済み）。
- **飾りだけの行（`★ ★` など）**（`NOISE_RE`）— 表の末尾に置かれ、直前の予定の行事名に混ざる。
  **ハイフンは対象に入れないこと。**「静岡 - KM東京」の対戦表記が壊れる。
- **祝日名は行ごと捨てず語だけ消す** — 「地域訪問 秋分の日」のように本来の行事と同じ欄に並ぶため。
- **年度外の日付は勝手に直さない** — ⚠印を付けて一覧で確認させる（作者が意図的に載せている場合がある）。

## 英語の予定表（`opts.lang` が `en`/`in` のときだけ通る層）

**英語でもGoogleのOCRは表を「1セル＝1行」で返す**（実データで確認済み）。だから行を集める仕組みは
日本語と共通で、差し替えたのは日付・時刻・年・区切り語だけ。日本語側のコードは一切通らない。

- **月名の直後に `\b` を要求すること。** 無いと `Term 1 marks` の `1 mar` を3月1日と読む（実際に発生）。
- **日の直後に `(?!\d)` を要求すること。** 無いと見出しの `MAY 2027` を5月20日と読む。
- **年度で繰り上げない。** 英語に年度は無い。入力された年をそのまま使う。年度のつもりで4月始まりに
  すると ICC日程の `7 Feb` が翌年になる。⚠の判定も暦年で見ること。
- **12時間制**（`7:30 PM`）。24時間制と取り違えると 19:30 が 07:30 になる。
- **月見出しから月・年を引き継ぐ**（`enDayLine`）— 英国の大学暦は `SEPTEMBER 2026` の下に `07` と
  日だけが並ぶ。歯止めは2つとも必須：**月見出しを見たあと**、かつ**行全体がちょうど1〜2桁の数字**。
  数字だけの行はどこにでもあるので、片方でも外すと誤爆する。
- **行事名の途中の曜日は消さないこと。** `University holiday - Easter Monday` `Good Friday` が壊れる。
  消してよいのは**行全体が曜日**のときだけ。
- **場所は見出しの列構成から決める**（`VENUE_HEAD_RE` / `REGION_HEAD_RE`）— 英語には都道府県のような
  手がかりが無い。見出しの**末尾に場所の列が何本あるか**を数え、行の末尾から同じ本数を場所にする。
  `Date/Match/Venue`＝1本、`…/都道府県/会場`＝2本、`…/Venue/Organiser/Contact`＝0本（発動しない）。
  都道府県ロジックを**先に**試すこと。順序を逆にすると `神奈川県 茅ケ崎総合体育館` が会場だけになる。

### 日付の書式は言語で衝突する（自動判定できない理由）

`12/04/26` は3通りに読める。**だから `window.LANG` で切り替えている**（自動判定は採らなかった）。

| 読み方 | 結果 |
|---|---|
| 日本語 `YY/MM/DD` | 2012-04-26 |
| 英・印 `DD/MM/YY` | 2026-04-12 |
| 米 `MM/DD/YY` | 2026-12-04 |

※英語側はまだ**スラッシュ表記に未対応**（月名のある表記だけ）。実装するなら、文書内に「最初の数が
12超」の行があれば日/月と確定できる。

### ポスター体裁は通常モードでは読めない

OCRが**列ごと**に返す（マッチ番号が全部→日付が全部→会場が全部…）。行の再構成は位置合わせに頼る
ことになり、1つずれると全件が別の予定になって**気づけない**。**AIモードへ回すこと。**
IPLのPDFは1〜2ページが行ごと、3ページが列ごとで、同じ文書の中で並びが変わる例。

## カレンダー登録の決め事

- 終日予定の終了日は**翌日**（Google仕様で排他的）。
- 時刻ありで期間なし → 1時間の予定。23時台は終了が翌日になる。
- **期間と時刻の両方があるときは期間を優先**（失うと痛いのは日数のため）。
- 登録できた行はチェックを外して二重登録を防ぐ。

## 検証済みの実データ（4枚）

| 予定表 | 形式 | モード | 結果 |
|---|---|---|---|
| 水泳競技スケジュール | 3列の表 | 通常 | 89件 |
| 四国大会予定表 | 期間主体 | 通常 | 42件 |
| リーグ戦日程 | 2列・時刻つき | 通常 | 150件 |
| 学校の年間行事予定表 | **マス目カレンダー** | **AI** | 157件・原本と照合し一致 |

マス目の見分け用（通常モードでは読めないのが正しい。`looksLikeGrid` が true になること）:

| 予定表 | 形 |
|---|---|
| 学校の年間行事予定表（`ocr-school-grid-2026.txt`） | 12か月×31日のマトリクス。日付と曜日が交互 |
| 学校の行事予定表（`ocr-school-grid2-2026.txt`） | 日/曜/行事の列が6か月分。曜日と行事名が同じ行に来ることも。**AIモードで211件**（実機確認） |

英語（`schedule/test/` に採取済み。`node schedule/test/run.js` で確認できる）。
**通常モードとAIモードの結果が一致することを実データで複数回確認している。**
まったく別の経路（OCR＋正規表現／AIが画像を直接読む）が同じ答えを出すので、
どちらかを直したときの答え合わせに使える:

| 予定表 | 特徴 | 結果 |
|---|---|---|
| ICC T20 World Cup | `7 Feb`（年の記載なし）・3列 | 55/55。**`/schedule-en/` の実機でも55件を再現** |
| 英・大学の term dates | 月見出し＋日だけ | 40/40 |
| IPL（表形式PDF） | 1〜2ページは行ごと、3ページは列ごと | 59/70（3ページは取得不可） |
| ASA（南ア陸連） | 7列・連絡先の列あり・日付が2行に割れる | 39/約50。行事名に主催者名が残る |
| IPL（ポスター） | 列ごと | **通常モードでは不可。AIモードへ** |

## 残っている作業

いまのところ無し。

一連の流れは実機で確認済み（マス目の文書を通常モードで読む → ⚠警告 → 「AIで読み直す」→
AIモードに切り替わって211件を正しく読み取り）。

周知サイト（dropper-tools.com）は3言語とも対応済み。トップのシリーズ欄に予定表ドロッパーの
カードがあり、プライバシーポリシーにも**AIモードの送信内容の違い**（予定表ドロッパーだけは
ファイルそのものを Gemini に送る）が3言語で反映されている。

一括登録は実機で確認済み（期間予定の最終日が含まれること、時刻ありが1時間になることを
Googleカレンダー上で確認）。マス目の見分けも実装済み（上の「マス目のカレンダーは、聞かずに見分ける」）。

（見送った案）**表形式のときはAIにもOCRテキストを送る** — 15MB超のファイルや無料枠切れの
救済になり、送信量も小さい。ただしAIモードを選ぶのは通常モードで失敗した人なので、
効く場面が限られる。画面の作り直しと同時にやると切り分けができなくなるため今回は入れていない。

---

# 決めごとドロッパー（`decide/`）— ドロッパーシリーズ第3弾

**会話のスクショ・打ち合わせメモ・議事録から、決まったこと／まだ決まっていないこと／やること／記録に
仕分けする**ツール。既存2本が「予定を取り出す」のに対し、こちらは「**話の結論を取り出す**」。

公開URL: https://app.dropper-tools.com/decide/ （en: /decide-en/ 、Hinglish: /decide-in/ ）

## 構成

`decide/` `decide-en/` `decide-in/` の3フォルダ。**3言語同期の鉄則がそのまま効く**。

- `decide/index.html` — 画面
- `decide/decide-i18n.js` — 多言語辞書。**イベント側・予定表側とは別物**（辞書は跨がせない）
- `decide/decide-ai.js` — 抽出（`window.DecideAI`）
- `decide/decide-app.js` — 画面制御・カレンダー登録・画像書き出し

## AI専用（通常モードが無い）

**会話は意味を解釈しないと決定／未決が分けられないため、正規表現モードは成立しない。**
既存2本の「通常モード／AIモード」の2択がここには無い。

その結果 **Googleドライブを通らないので、読み取りだけならログインが要らない**。
カレンダー登録を押したときだけログインを求める。

- **スコープは `calendar.events` だけ。** ドライブを使わないので `drive.file` も `drive.appdata` も要らない。
  既存2本より小さい同意で済む。**新しいスコープは足さないこと**（審査の項を参照）。
- クライアントID・APIキーの保存先（`dropper_ai_key`）は既存2本と共通。

## 仕分けの判定軸は「これから × 確定か」（`decide-ai.js`）

**「誰が決めたか」では分けない。** 誰の発言でも、予定として確定していれば決まったこと。

| | 確定している | まだ確定していない |
|---|---|---|
| これからのこと | `decided`（決まったこと） | `undecided`（まだ決まっていないこと） |
| すでに済んだこと | `records`（記録） | `records` |

- **済んだ出来事を決定事項に格上げしない。** 文末が「〜しました」「〜だった」なら `records`。
  「〜予定」「〜する」「〜と決定した」なら `decided`。報告書・議事録を読ませると効いてくる。
- **`records` は捨てない。** あとから見返す控えとして残す（日付・金額・連絡先・型番などを含めて）。
- **会話は二転三転する。** 後の発言が前を打ち消していたら、最終的な結論だけを `decided` に入れる。
- **推測で決定事項を作らない。** 書かれていなければ `undecided` に置く。
- 二重線・取り消し線で消された項目は打ち消されたものとして扱う（手書きメモ対策）。

## カレンダー登録

- 対象は**日付のある `decided` と `todos` だけ**。`records` はチェックを出さない（済んだことなので）。
- 日付だけで時刻を伴わないため**終日予定**。終了日は+1（Google仕様で排他的。既存2本と同じ）。
- 相対的な書き方（「来週の火曜」）から直した日付は、**元の言い方も並べて出す**。
  起点になる「基準日」の既定は今日。空のままにしない。

---

# LINE連携（`line/`）— LIFFランチャー

LINEのトークからドロッパーを開くための1枚（`line/index.html`）。日本語のみ・`noindex`。
公式アカウントの友だち追加を促しつつ、ツール本体へは**外部ブラウザで**送る。

## なぜ外部ブラウザに送るのか（最重要）

**GoogleはLINE内ブラウザのようなWebViewからのOAuthを拒否する**（`disallowed_useragent`）。
LINE内で開くと**イベント・予定表はログイン画面で止まる**。快適さの問題ではなく、
動くか動かないかの問題。

- ランチャーは `liff.openWindow({ url, external: true })` で外部ブラウザへ渡す。**2026-08-04 に実機確認済み。**
- リッチメニュー等からツールへ直接リンクするときは **`?openExternalBrowser=1` を必ず付ける**。
  付け忘れると「タップしたけど使えなかった」という最悪の初回体験になる。
- 決めごとドロッパーは読み取り・コピー・画像保存までLINE内でも動くが、その中で
  「カレンダーに登録」を押すと同じ壁に当たる。**壊れる経路を残さない**ため3本とも外部へ送っている。

## 構成

| ファイル | 役割 |
|---|---|
| `line/index.html` | LIFFランチャー（利用者が開く。日本語のみ・`noindex`） |
| `line/menu.html` | **作業用**。リッチメニュー画像（2500×1686）をその場で描いてPNGで落とす |
| `line/greeting.html` | **作業用**。あいさつメッセージに添える「要項の送り方」画像を描いてPNGで落とす |

作業用の2枚は**作者しか開かない**。`noindex` を付け、GA4タグは入れない。

**どちらも寸法や配置を固定値で持たない。** 文言を足したときに黙ってはみ出す。
`menu.html` は `fitFont` で字を縮め、`greeting.html` は
キャンバス高さ・カード高さ・見出し幅をすべて中身から算出している
（固定値のせいで3回はみ出しを出した）。文言を変えるときは先頭の配列だけ触ればよい。

**`greeting.html` の横長（1800×838・縦横比2.15）には理由がある。触るなら読むこと。**
LINEのトークは画像に**高さの上限**をかける（テキストの吹き出しは行数で伸びるが画像は伸びない）。
縦横比 ≒ 2.15 が、吹き出しの幅と高さを同時に使い切る値。
これより縦長にすると高さの天井で幅が縮み、横長にすると幅の天井で高さが縮む＝どちらも文字が小さくなる。
**ピクセル数を増やしても表示は変わらない**（縮小されて戻るだけ）。文字を大きくする唯一の手は中身を減らすこと。

## 設定値

- LIFF ID: `2010974942-3bU1tiQJ` / LIFF URL: `https://liff.line.me/2010974942-3bU1tiQJ`
- 公式アカウント: `@937eheeb`（ドロッパー）
- **LIFF ID は秘密情報ではない**（LIFF URL にも出る）。チャネルシークレットはこの構成では使わない。
- Scope は `openid` のみ。`profile` は取らない（`liff.getProfile()` を呼んでいない）。
- 友だち追加オプションは `aggressive`。**LIFFアプリ側の設定**で、コードでは指定しない。

## ハマった実例（2026-08-04）

**公式アカウントの「Messaging API」を有効にしないと、チャネルの
「リンクされたLINE公式アカウント」の一覧に現れない。** リンク先は実質
Messaging APIチャネルを指すため。権限やログインアカウントの不一致を疑って
数往復したが、原因はこれだった。**一覧が空なら、まずMessaging APIの有効化を確認する。**

## 壊さないための決まり

- **既存3本にLIFF SDKを読ませない。** LINE以外から来る大多数に余計なスクリプトを
  読ませることになり、3言語同期の対象にも新しい依存が増える。LINE利用者だけが
  この1枚を通る形にしてある。
- **`LIFF_ID` が空でも、SDKが読めなくても、`init` が失敗しても素のリンクとして動く。**
  LINE外から開いた人には何も起きない（`isInClient()` が false のとき何もしない）。切り分けにも使える。
- ミニアプリ（審査あり）ではなく**通常のLIFF**。審査・認証を経ても得られるものはほぼ同じで、
  発見面の優遇は期待できないと判断した。
- `robots.txt` は `/line/` を **`Disallow` にしない**。`noindex` 自体を読めなくなるため。

---

# ⚠️ 絶対に変更してはいけないもの（Google OAuth審査）

2026-07-19 に `calendar.events`（機密スコープ）の本番審査に**承認済み**。
以下を変更すると**新たな検証申請が必要**になり、承認済みの状態が壊れる。

## 1. OAuthスコープを増やさない（最重要）

`app.js` の `BASE_SCOPES`:

```js
var BASE_SCOPES = [
  'https://www.googleapis.com/auth/drive.file',
  'https://www.googleapis.com/auth/drive.appdata',
  'https://www.googleapis.com/auth/calendar.events'   // ← 審査承認済みの機密スコープ
];
```

- **この配列に新しいスコープを足さない。**
- 新機能に別のスコープが必要になった場合は、**実装せずに必ず先にユーザーへ確認する**。
  「その機能にはスコープ追加が必要で、Googleの再審査（数週間）が発生します」と伝えること。
- `CLUB_EXTRA_SCOPES`（`spreadsheets`）は**撤去済み**。`?club=hakusan` のクラブモードごと、
  出欠システム連携（下記）に置き換えた。**復活させない**——出欠データは主催者自身のGASが書くので、
  ドロッパー側にスプレッドシートの権限は要らない。
- 出欠システム連携の通信相手は主催者のGASウェブアプリだけで、**Googleの権限は一切使わない**。
  この機能を広げるときもスコープは増やさないこと。

## 2. 同意画面の登録内容と食い違わせない

Googleに登録済み: アプリ名「ドロッパー」／ホームページ `https://dropper-tools.com` ／
プライバシーポリシー `https://dropper-tools.com/privacy.html`

- 承認済みJavaScript生成元は `https://app.dropper-tools.com` のみ。
  別ホストで動かす前提のコードや、生成元の追加を必要とする変更を入れない。

---

# ハマり所（既知）

## LINEでのリンク共有（案内文ジェネレーター）

案内文に貼るリンクには、実測で判明した2つの制約がある。

1. **`%` を含むURLはLINEが壊す** — 本文中URLの `%`（日本語のエンコード部分）をタップ時に再エンコードし、
   Googleに渡るタイトル・場所が文字化けする。
   → 対策済み: 案内リンクは `%` を含まない base64url に符号化し、`add/index.html` の中継ページ経由で
   正規のGoogleカレンダー／地図URLへ組み立て直す。**この経路を外さない。**
2. **長いURLはリンク化されない** — 青下線にもタップ可能にもならず、ただの文字列になる。
   実測: 128字・237字＝OK、453字＝NG。
   → カレンダー追加リンクは **大会名＋日程＋会場名のみ**に保つ。詳細（試合形式・締切・住所・
   key_info）をリンクに詰め込むと再発する。
3. **LINE直行ボタン（`line.me/R/msg/text/?`）経由でも中継リンクは壊れない** — 2026-08-02 実機確認。
   本文全体をURLエンコードして渡す経路だが、中継リンクは base64url で `%` を含まないため、
   1 の二重エンコードは起きない。**この経路を疑って再検証しなくてよい。**
   逆に言えば、中継リンクに `%` が入る形へ変えると、この経路でも 1 が再発する。

## GitHub Pages

コミットしても**古いファイルが配信され続ける**ことがある。原因は build ジョブ失敗 → deploy が Skipped。
再アップロードで解決する。配信中の実物は `https://app.dropper-tools.com/calendar/` を直接取得して確認する
（ブラウザキャッシュと切り分けるため）。
※ 2026-07-26 にルートへ `.nojekyll` を設置済み。Markdown 追加による Jekyll ビルド失敗はもう起きない
（Pages のデプロイ自体が失敗するケースは引き続きあり得る）。

## AI（Gemini BYOK）

- APIキーはユーザー自身のもの。`localStorage` の `dropper_ai_key` に端末内保持し、サーバーへ送らない。
- **APIキーを求める前に「クレカ不要・無料枠内なら0円・取得は約3分」を必ず見せる**（`aiBadge*` の3バッジ）。
  入力欄の下には「入力したキーの扱い」（`key-trust`）で保存先・当方がサーバーを持たないこと・
  送信先・やめ方を並べる。**ここを削ると「キーを入れさせられる」印象に戻る。**
  3本のドロッパー・3言語で同じ内容にそろえてある。
- 取得ガイドのリンク先は周知サイトの `https://dropper-tools.com/apikey.html`（AI Studio 直リンクではない）。
  **`href` は `applyDom` が `window.LANG` を見て張り替える**（`aiKeyGuideLink` と `keyMoreLink`）。
  HTMLに直書きの値は ja 用の既定値。en の利用者を ja のページへ送らないための仕組みなので外さないこと。
- 抽出プロンプトは `promptJa` / `promptEn` を `window.LANG` で選択する。
  **en/in 版で日本語プロンプトを使わないこと**（英語要項が日本語に翻訳されてしまう）。
- レート制限対策で約5秒のスロットルを入れている（`AI_MIN_INTERVAL_MS`）。外さない。

### キー入力欄（マスク表示と接続テスト）

3本のドロッパー・3言語で同じ作り。`app.js` の `askAiKey_` / `schedule-app.js` ・ `decide-app.js` の `askKey` にある。

- **マスク（`type=password`）と表示切り替え（目のボタン `#keyEyeBtn`）は必ずセットで置く。**
  伏せるだけにすると、貼り付けた内容を目視できず**かえって不安になる**。
- **`label` で `input` を包まないこと。** 包むと目のボタンのクリックが `label` に吸われる。
  `<label for="keyInput">` と入力欄を分けてある。
- 接続テストは**キーの形（`AIza` + 英数）が整った0.6秒後に1回だけ**。1文字ごとには叩かない。
  結果は `#keyCheck` に出す（`keyTest*` の7キー）。誤ったキーでも保存でき、
  **実際にドロップするまで気づけなかった**のを直したもの。
- **検証に `generateContent` を使わないこと。** 無料枠の1日あたり回数（RPD）を検証だけで消費する。
  叩くのは `ListModels`（`GET /v1beta/models`）。こちらはこの枠を消費しない。
- **モデル名は文言に直書きせず `AI_MODELS` / `SchedAI.MODELS` から採る**（`keyTestOk` の `{model}`）。
  一覧に出なくても失敗扱いにしない（有効なキーに✕を出す方が害が大きい）。
- **接続テストがNGでも保存は通す。** 通信エラーで保存できないと、そこで詰んでしまう。
- 予定表側は「AI設定を変更」で保存済みキーが最初から入るので、**開いた時点で1回テストする**
  （そのキーがまだ生きているか分かるように）。

## 出欠システム連携（出欠ドロッパー）

カードの **「🙋 出欠システムに保存」** で、読み取った要項を出欠システム（`attend/`）へ渡す機能。
**2026-08-12 に旧クラブモード（`?club=hakusan` の大会マスタ・シート書き出し）を置き換えて導入し、
2026-08-22 に受け渡しを作り直した。** 旧モードのコードは残っていない。

- **ドロッパーは何も書き込まない。** 押すと、読み取り結果を積んだ管理画面
  （`attend/admin.html#t=<トークン>`）を新しいタブで開くだけ。取り込むかどうかは向こうの画面が決める。
  **そのためこの画面に設定が一切ない**（デプロイID・書き込みキー・設定モーダルは撤去ずみ）。
- **ボタンの置き場所**（2026-08-23 に入れ替えた）
  - カードの中：「案内文を作る」と「Googleカレンダーに登録」を`.ann-row`で横並び。
    **カレンダー登録はそのカード1枚だけ**を登録する（まとめて登録は廃止）
  - 画面の下（`#bar`）：「🙋 出欠システムへ」1本。**チェックの入った1枚**を送る。
    2枚以上チェックされていたら止める（出欠は行事1件につき1つなので、まとめて送っても手数が増えるだけ）
  - `#bar` は出欠が使える言語（ja）でしか出さない。`showBar_()` を通すこと
- **カードの列挙は `cards_()` を通す。** `items` は読み取りの流れで積まれるが、カードだけ
  先にできることもある。**画面を正とする**ほうがボタンの出し分けを間違えない（`li.__cardApi`）
- 符号化は `calendar/attendance-hook.js` の `saveToken` / `adminUrl`。原本は
  `hakusan-attendance` の `dropper/attendance-hook.js`。**受け取る側の `importTaikai` と対なので、
  形を変えるときは両方そろえる。**
- **★ タブは押した瞬間に開くこと。** 要項の保存を待ってから `window.open` を呼ぶと
  ポップアップブロックに止められる。先に空のタブを開き、文字列ができてから `location.replace`。
  クリップボードの「ユーザー操作中でないと書けない」（iOS Safari）とまったく同じ罠。
- 開けなかったときだけクリップボードに落とす（貼り付けてもらう）。こちらは
  **Promise を渡せる `ClipboardItem`** で書く。`await` してから `writeText` は黙って失敗する。
- **要項のドライブ保存はカレンダー登録と切り離してある**（`attendEnsureYoukou_`）。
  カレンダーを使わない主催者にも「要項を見る」を出すため。保存に失敗しても止めない。
- 案内文には**URLを入れない**。地図・カレンダー・要項は出欠の回答画面にボタンとして出る。
  出欠を保存していない回だけ、今までどおり地図とカレンダーのURLを載せる。
- 種目の取り出しは旧マスタ書き出しの `attendEvents_`（旧 `masterEvents_`）をそのまま流用している。
  実チラシで詰めた判定なので作り直さないこと（`NON_EVENT_RE` で「予選リーグ」等の競技方法を弾く）。
- **失敗しても要項を再ドロップさせない。** ボタンは必ず押しなおせる状態に戻す
  （再ドロップするとカレンダー予定が重複する）。同名・同開催日は向こうが作り足さず既存を返す。
- v1は**日本語版のみ表示**（`app.js` の `ATTEND_AVAILABLE`）。`attend/` が日本語のため。
  i18n キーは3言語ぶん揃えてあるので、`attend/` を訳したら `ATTEND_AVAILABLE` を外すだけで出せる。

## その他

- ツール本体に `<footer>` は無い（`<header>` と `<main>` のみ）。導線はヘッダーに置く。
- ヘッダー最上部の「🎉 今なら無料公開中」バッジは i18n キー `freeBadge`（CSS: `.free-badge`）。
  外す/差し替えるときは **3言語同時**に行う。課金導入時に削除予定。

---

# 検討して見送った案（同じ議論を繰り返さないために）

2026-08-03〜04 に検討した結果。**判断材料は簡単には再発見できない**ので残す。
「なぜやらないか」が書いてあるものは、蒸し返す前にここを読むこと。

## 見送ったもの

| 案 | 見送った理由 |
|---|---|
| **LINEミニアプリ** | 審査と認証済みアカウントが要る。しかも**ミニアプリはLINE内WebViewが前提**で、Googleが WebView からの OAuth を拒否するため**3本のうち2本はログインできない**。発見面の優遇も期待できない。**通常のLIFF（審査なし）で友だち追加オプションは使え、同じ利益が得られる** |
| **GASでLINEボット** | Webhook は GAS のウェブアプリで作れるが、**BYOKが成立しない**。利用者ごとのキーを使えず全員分を運営のキーで処理することになり、無料枠の負担が運営に集中する。「利用者が自分のキーを使う＝運営コストゼロ」という設計思想と正面からぶつかる |
| **レシピドロッパー** | Google Keep に公開APIが無く、構想の中心だった「買い物リスト化」が実装できない。レシピ本の調理手順は著作物 |
| **手書きメモドロッパー（単独）** | 出力がテキストだけなら**Geminiアプリで写真を撮るのと同じ**。決めごとドロッパーに畳み込んで解決した（出力が4つの節になるので、この反対理由が消える） |

## 保留（条件が変われば有効）

| 案 | 状況 |
|---|---|
| **領収書ドロッパー** | 痛みは本物。ただしインボイス登録番号や8%/10%は日本固有で**3言語展開が効かない**。国税庁の適格請求書公表システムWeb-APIで登録番号を照会できれば差別化になるが、ブラウザから叩けるか（CORS）は未検証 |
| **スクショから要約・共有リンク** | 共有リンクは**サーバーが要る**ため不可。要約だけなら Gemini アプリと同じ |
| **シェアターゲットピッカー** | LIFFランチャーに「友だちに教える」を付けられる。`chat_message.write` が要り、ブラウザ最小化が無効になる副作用がある |

## 次の候補

**名刺ドロッパー（VCF出力）**。**権限ゼロで実現でき、3言語展開も効く**唯一の案。
Google Contacts API は制限付きスコープで再審査が必要になるが、**VCFファイルを生成するだけなら
スコープもログインも要らない**（iPhone・Android・Googleコンタクトのどれでもタップ一発で取り込める）。
予定表ドロッパーの「1枚＝N件を一覧で確認してから出力」の構造がそのまま使える。
懸念は、投げ込まれるのが**他人の個人情報**である点。

## 調査済みの事実（着手時の前提が変わるもの）

- **`drive.file` だけで Google スプレッドシートを作成・追記できる。**
  `spreadsheets.create` と `spreadsheets.values.append` の認可スコープに `drive.file` が
  含まれている（公式リファレンスで確認済み）。**`spreadsheets` スコープの追加も再審査も不要。**
  ただし**ファイル単位の権限**なので、アプリが作ったシートにしか触れない
  （利用者が手で作った既存シートへの追記は Picker 経由でないと不可）。
- 上記の論点だった `CLUB_EXTRA_SCOPES`（`spreadsheets`）は、**2026-08-12 に不要になった**。
  クラブモードごと出欠システム連携に置き換え、出欠データは主催者自身のGASが書くようにしたため、
  ドロッパー側にスプレッドシートの権限自体が要らなくなった。

---

# 課金導入（計画中）

- 買い切り課金を計画中。**設計メモはローカルの `BILLING-DESIGN.md`**（`.gitignore` 済み。
  内部資料のため**公開リポジトリにコミットしない**）。
- 方式の要点: 決済は Dodo Payments、利用回数はユーザー自身の Drive appDataFolder に保存、
  GAS はライセンス発行・検証のみ。**OAuthスコープは一切追加しない**（詳細はローカルメモ参照）。

---

# 主要な定数・ID

- Cloud Console プロジェクトID: `dropper-499204`（組織 movie-pingpong-org）
- ROOT_FOLDER_ID: `1q_3cknEoJzFezW3yuiLTKQ4mo1siicQM`
- REVIEW_SHEET_ID: `10p2LrxC01UpvjTekBF_wiS9kvWLXWcgmYGWKYBdIczE`
- AI_MIN_INTERVAL_MS: `5000`（Gemini RPM対策のスロットル）
- 白山クラブ用GAS版: `hakusan_calendar_v17k.gs`（抽出ロジックはWeb版 `parser.js` と同期が必要）

---

# 進め方（ユーザーの好み）

- **一気に全部やらず、区切りごとに確認を取る。**
- 変更したら、**確認URLと確認ポイント**をセットで伝える。
- 3言語まとめて扱う。
